{
    "schema": 1,
    "description": "This plugin parse a CYPHER query result to a JSON object. This plugin is usually called after a CYPHER query plugin.",
    "execution_settings": {
      "default": {
        "max_tokens": 1000,
        "temperature": 0.1,
        "top_p": 1.0,
        "presence_penalty": 0.0,
        "frequency_penalty": 0.0
      }
    },
    "input_variables": [
      {
        "name": "query",
        "description": "A CYPHER query",
        "default": ""
      },
      {
        "name": "question",
        "description": "The user's question related to the CYPHER query",
        "default": ""
      },
      {
          "name": "result",
          "description": "The CYPHER query result",
          "default": ""
      }
    ]
  }